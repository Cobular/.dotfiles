

# Wasienv
export WASIENV_DIR="/home/cobular/.wasienv"
[ -s "$WASIENV_DIR/wasienv.sh" ]; and source "$WASIENV_DIR/wasienv.sh"

# Wasmer
export WASMER_DIR="/home/cobular/.wasmer"
[ -s "$WASMER_DIR/wasmer.sh" ]; and source "$WASMER_DIR/wasmer.sh"
